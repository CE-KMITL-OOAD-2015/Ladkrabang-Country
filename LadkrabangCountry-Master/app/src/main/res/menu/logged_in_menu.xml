<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <item android:icon="@mipmap/menu" android:id="@+id/action_settings"
        android:orderInCategory="100" android:title="setting" app:showAsAction="always">
        <menu>
            <item android:id="@+id/profile"
                android:icon="@mipmap/guest_icon"
                android:title="Name"
                />
            <item android:id="@+id/logout_icon"
                android:icon="@mipmap/logout_icon"
                android:title="Logout"
                />
        </menu>
    </item>

</menu>
